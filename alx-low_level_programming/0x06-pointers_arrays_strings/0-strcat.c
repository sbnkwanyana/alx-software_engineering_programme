#include "main.h"
#include <string.h>

/**
 * _strcat - function to concatinate two strings
 * and return pointer to destination
 * @dest: string destination
 * @src: string source
 * Return: Char *
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
