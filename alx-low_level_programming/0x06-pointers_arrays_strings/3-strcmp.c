#include "main.h"
#include <string.h>

/**
 * _strcmp - compare 2 strings with each other
 * @s1: string to compare
 * @s2: string to compare with
 * Return: int
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
